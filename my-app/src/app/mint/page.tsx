import { NextPage } from "next";
// next 내장 페이지 라우팅. http://localhost:3000/mint 로 들어가서 확인
// 폴더명이 경로가 됨 (폴더명은 소문자가 컨벤션)
const Mint: NextPage = () => {
  return <div>Mint</div>;
};

export default Mint;
