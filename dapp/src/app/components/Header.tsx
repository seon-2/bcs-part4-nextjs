import { FC } from "react";

const Header: FC = () => {
  return (
    <div className="bg-green-100 py-4 pr-8 flex justify-end">
      <button>지갑로그인</button>
    </div>
  );
};

export default Header;
